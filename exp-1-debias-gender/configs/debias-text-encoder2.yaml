proj_name: gender-debias-text-encoder-again
train_text_encoder: True
train_unet: False
weight_loss_img: 8
weight_loss_face: 4
factor1: 0.2
factor2: 0.2
uncertainty_threshold: 0.2
train_images_per_prompt_GPU: 12
train_GPU_batch_size: 3
val_images_per_prompt_GPU: 8
val_GPU_batch_size: 8
val_images_per_prompt_total: 60   # 8 GPU * 8 = 64 generated, keep exactly 60 per occupation
learning_rate: 5e-5
max_train_steps: 10000